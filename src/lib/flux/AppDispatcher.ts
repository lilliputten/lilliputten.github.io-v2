import { Dispatcher } from 'flux';

// const AppDispatcher = new Dispatcher();
//
// export interface IAppDispatcher<P> extends Dispatcher<P>;

// const timestamp = Date.now();
// console.log('AppDispatcher', timestamp, AppDispatcher);

// // tslint:disable-next-line
// console.log(AppDispatcher, window); // tslint:disable-line
// debugger;

export default new Dispatcher();
